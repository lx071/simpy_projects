#include <iostream>
#include "sim_utils.h"

using namespace std;

int main()
{
    cout << "SIM" << endl;
    getHandle("sim_wrapper");

    // setValue(signal_id["clk"], 0);
    // setValue(signal_id["reset_n"], 1);

    // setValue(signal_id["A"], 2);
    // setValue(signal_id["B"], 3);
    // setValue(signal_id["op"], 1);
    // setValue(signal_id["start"], 1);

    // eval();
    // sleep_cycles(5);

    // int A = getValue(signal_id["A"]);
    // int B = getValue(signal_id["B"]);
    // int result = getValue(signal_id["result"]);

    
    // printf("A: %0d, B: %0d, result: %0d", A, B, result)
    
    return 0;
}